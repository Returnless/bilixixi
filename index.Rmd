---
title: "Bilixixi"
#author: "☁️B站罗云熙首页拯救计划☁️"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: scroll
    theme: bootstrap
    logo: BilixixiLogo.png
    navbar:
       - { title: "首页", href: "http://bilixixi.cc/index.html", align: left }
       - { title: "帮助", href: "http://bilixixi.cc/help.html", align: left }
       
    includes:
       in_header: GA.html
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE,message=FALSE,warning=FALSE, cache=FALSE)
library(DT)
library(stringr)
source("Bilixixi.R")

```


Column {data-width=200}
-----------------------------------------------------------------------



Column {data-width=650}
-----------------------------------------------------------------------


<center>

<h1> 哔哩熙熙动画!</h1>
<h4>☁️B站罗云熙首页拯救计划☁️</h4>

支持排序·筛选·搜索功能 | 已人工屏蔽部分路透 | 支持8月16日后上线作品

</center>


```{r vie}
# vie[,c("titlelink","lengthnum","viewnum","text","upload","uplink")]
htmltools::tags$script(src = "mysetting.js")

datatable(vie, rownames = FALSE, filter = 'top',style ="bootstrap",
#####################
          options = list(
            search = list(regex = TRUE, search = ''),
                        pagingType="numbers",
                        pageLength = 30,
                        language = list(search = '搜索:',
                        lengthMenu='每页显示 _MENU_ 个视频',
                        zeroRecords='什么也没找到，换个关键词试试',
                        info='现在第 _PAGE_ 页 共 _PAGES_ 页',
                        infoEmpty='没有可显示的页面',
                        infoFiltered='(从 _MAX_ 项中搜索)',
                        paginate=list(previous='上一页',
                        'next'='下一页',
                        first='第一页',
                        last='最后一页')),
                        
                        columnDefs = list(
                        list(targets=1,
                        render = JS(
                     "function(data, type, row, meta) {",
                     "return type === 'display'?",
                     "'<span title=\"'+data +'\">'+Math.floor(data/60) + '分'+Math.floor(data%60)+'秒</span>' : data;",
                     "}")
              ),
              list(targets=2,
                   render = JS(
                     "function(data, type, row, meta) {",
                     "return type === 'display' && data>=10000 ?",
                     "'<span title=\"' + data + '\">'+Math.floor(data/1000)/10+'万</span>' : data;",
                     "}")
              ),
               list(targets=2,
                   render = JS(
                     "function(data, type, row, meta) {",
                     "return type === 'display' && data>=10000 ?",
                     "'<span title=\"' + data + '\">'+Math.floor(data/1000)/10+'万</span>' : data;",
                     "}")
              ),
              list(targets=4,
                   render = JS(
                     "function(data, type, row, meta) {",
                     "return type === 'display' ?",
                     "'<span title=\"' + data + '\">' + data.substr(5, 12) + '</span>' : data;",
                     "}")
              ),
              list(targets= '_all',
                createdCell=JS(
                  "function (td, cellData, rowData, row, col) {",
                    "$(td).css('padding', '8px 4px 8px 4px')",
                  "}")
              ),
              list(targets=1:4,
                   class="dt-body-center"),
              list(targets='_all',
                   class="dt-head-center")
          )),
          escape = FALSE,
          colnames = c("标题","时长","观看","弹幕","日期","UP")
          )

```

最近更新: `r lastdate` | 微博: [\@Re_空中三日](https://www.weibo.com/6722029638/)


Column {data-width=200}
-----------------------------------------------------------------------

